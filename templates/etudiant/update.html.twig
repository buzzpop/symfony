{% extends 'base.html.twig' %}
{% block title %}Hello ChambreController!{% endblock %}

{% block body %}
    <div class="container-fluid w-100 bg-light p-3">
        <h1>Liste des Etudiants</h1>
        <p><a href="{{ path('create-etudiant') }}">Ajouter un Etudiant</a></p>

        <div class="container w-50 bg-light p-3">
            <div class="row justify-content-center"><h3>Enregistrement Etudiant <i class="fa fa-user" aria-hidden="true"></i></h3></div>
            {{form_start(formmodif)}}
            {{form_widget(formmodif)}}
            <button type="submit" class="btn btn-outline-dark">Enregistrer</button>

            {{form_end(formmodif)}}
        </div>
    </div>
{% endblock %}
